@model int
@{
	var hours = Model / 60;
	var minutes = Model % 60;

	if (hours == 1)
	{
		@:1 time
	}
	else if(hours > 1)
	{
		@:@hours timer
	}

	if(hours > 0 && minutes > 0)
	{
		@:og
	}

	if (minutes == 1)
	{
		@:1 minutt
	}
	else if (minutes > 1)
	{
		@:@minutes minutter
	}
}
